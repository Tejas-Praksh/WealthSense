# TEMPLATE ONLY — replace CHANGE_ME values before applying in a real cluster.
# Prefer: kubectl create secret generic wealthsense-secrets -n wealthsense --from-env-file=...
apiVersion: v1
kind: Secret
metadata:
  name: wealthsense-secrets
  namespace: wealthsense
type: Opaque
stringData:
  POSTGRES_PASSWORD: "CHANGE_ME"
  REDIS_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME"
  CLAUDE_API_KEY: "CHANGE_ME"
  GEMINI_API_KEY: "CHANGE_ME"
  ENCRYPTION_KEY: "CHANGE_ME"
  RAZORPAY_KEY_ID: "CHANGE_ME"
  RAZORPAY_KEY_SECRET: "CHANGE_ME"
