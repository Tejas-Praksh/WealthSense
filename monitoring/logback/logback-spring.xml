<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <springProperty scope="context" name="appName" source="spring.application.name"/>
  <springProperty scope="context" name="activeProfile" source="spring.profiles.active" defaultValue="default"/>

  <!-- Console appender with JSON format for Docker logs -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
      <customFields>{"service":"${appName}","environment":"${activeProfile}"}</customFields>
      <includeCallerData>false</includeCallerData>
    </encoder>
  </appender>

  <!-- Logstash TCP appender for ELK ingestion -->
  <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
    <destination>logstash:5000</destination>
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
      <customFields>{"service":"${appName}","environment":"${activeProfile}"}</customFields>
    </encoder>
    <connectionTimeout>5000</connectionTimeout>
    <reconnectionDelay>10 seconds</reconnectionDelay>
  </appender>

  <!-- Async wrapper to avoid blocking the main thread -->
  <appender name="ASYNC_LOGSTASH" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="LOGSTASH"/>
    <queueSize>512</queueSize>
    <discardingThreshold>0</discardingThreshold>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ASYNC_LOGSTASH"/>
  </root>

  <!-- Reduce noise from Hibernate and Spring -->
  <logger name="org.hibernate.SQL" level="WARN"/>
  <logger name="org.springframework.web" level="INFO"/>
  <logger name="org.springframework.security" level="WARN"/>
</configuration>
